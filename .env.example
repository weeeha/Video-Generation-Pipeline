# Copy to .env and fill in. .env is gitignored — never commit real keys.

# BytePlus ModelArk — Seedance 2.0.
# Key console: https://console.byteplus.com/ark  (region ap-southeast-1)
# Balance:     https://console.byteplus.com/finance
#
# NOTE: on Nick's Mac a working key already exists at
#   ~/Documents/Claude/Projects/SeedDance/.env
# and pipeline/seedance.py falls back to it automatically — this file is only
# needed on other machines / CI.
ARK_API_KEY=
